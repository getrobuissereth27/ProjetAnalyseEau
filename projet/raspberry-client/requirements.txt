requests>=2.31.0

# Nécessaire uniquement sur le vrai Raspberry Pi (matériel réel), pour lire
# le port série de l'Arduino. Sur ton ordinateur, laisse-le de côté : le code
# bascule automatiquement en mode simulation si absent (voir arduino.py).
pyserial>=3.5
